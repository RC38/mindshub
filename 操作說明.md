# mindshub 操作說明

> 本文件記錄 `RC38/mindshub`（fork 自 [mindsdb/mindshub](https://github.com/mindsdb/mindshub)）的完整操作經驗與日常流程。
> 建立日期：2026-09-19，分支：`branch-randy`

---

## 一、Repo 基本資訊

| 項目 | 值 |
|------|-----|
| 本地路徑 | `/Volumes/tf-1tb/googledriver/96.code/github/mindshub` |
| `origin`（自己的 fork） | `https://github.com/RC38/mindshub.git` |
| `upstream`（原始 repo） | `https://github.com/mindsdb/mindshub.git` |
| 主要分支 | `main` |
| 功能分支 | `branch-randy`（已 push，tracking `origin/branch-randy`） |

```bash
# 確認 remote 設定
git remote -v
# origin    https://github.com/RC38/mindshub.git (fetch/push)
# upstream  https://github.com/mindsdb/mindshub.git (fetch/push)
```

---

## 二、初始建立流程（已完成，供參考）

### 1. Fork + Clone

在 GitHub 網頁上對 `mindsdb/mindshub` 點 **Fork**（fork 到 RC38），然後：

```bash
cd /Volumes/tf-1tb/googledriver/96.code/github
git clone https://github.com/RC38/mindshub.git mindshub
cd mindshub
# 新增 upstream remote，之後才能同步原始 repo
git remote add upstream https://github.com/mindsdb/mindshub.git
```

### 2. 下載 Submodules（VS Code 資料樹上的「S」標記）

mindshub 是 monorepo，以下 4 個資料夾是 **Git Submodule**，各自指向獨立 repo：

| 路徑 | 來源 repo |
|------|-----------|
| `frontend` | [mindsdb/cowork](https://github.com/mindsdb/cowork) |
| `backend/core_api` | [mindsdb/cowork-server](https://github.com/mindsdb/cowork-server) |
| `backend/core_agent` | [mindsdb/anton](https://github.com/mindsdb/anton) |
| `backend/data-vault` | [mindsdb/data-vault](https://github.com/mindsdb/data-vault) |

clone 後預設**不會**下載子模組內容（資料樹顯示「S」標記、`git submodule status` 前面是 `-`）。下載方式：

```bash
# 初始化並下載全部子模組
git submodule update --init

# 只下載指定的
git submodule update --init frontend

# 查看狀態（前面是空白 = 已初始化；- = 未初始化）
git submodule status
```

> 💡 VS Code 圖形介面：點「S」標記旁的 **⋯** → Initialize Submodule，或 `Cmd+Shift+P` → `Git: Initialize Submodules`。

### 3. 建立功能分支

```bash
git checkout -b branch-randy main   # 從 main 分出並切換
git push -u origin branch-randy     # 推到 fork 並設定 tracking（之後直接 git push 即可）
```

---

## 三、日常同步流程（重點 ⭐）

> **目的**：把原始 repo（`mindsdb/mindshub`）的最新更新，同步到自己的 fork `main`，再合併到功能分支 `branch-randy`。
>
> **觸發方式**：對 Copilot 說「**同步 mindshub**」即可自動執行以下完整流程。

### 完整指令（依序執行）

```bash
cd /Volumes/tf-1tb/googledriver/96.code/github/mindshub

# ── 第 1 步：切回 main，從原始 repo 拉取最新 ──────────────
git checkout main
git fetch upstream                    # 只下載、不改動任何檔案（安全）

# ── 第 2 步：檢查是否有新 commit ───────────────────────────
git rev-list --count main..upstream/main
#   輸出 0  → 已是最新，同步結束（可跳過後面步驟）
#   輸出 >0 → 有 N 個新 commit，繼續往下

# ── 第 3 步：合併進本地 main 並推到 fork ───────────────────
git merge upstream/main               # 產生 merge commit，保留完整歷史
git push origin main                  # 同步到 RC38/mindshub 的 main

# ── 第 4 步：把更新後的 main 合併到功能分支 ────────────────
git checkout branch-randy
git merge main                        # 若無衝突會顯示 Already up to date
git push                              # 推到 origin/branch-randy（已 tracking）
```

### 流程圖

```
upstream ──fetch──▶ 本地 main ──push──▶ Fork main ──merge──▶ branch-randy ──push──▶ Fork branch-randy
(mindsdb)          (你電腦)            (RC38)              (功能分支)             (RC38)
```

### 遇到 merge conflict（衝突）怎麼辦？

1. Git 會列出衝突檔案，用 VS Code 打開，逐一選 **Accept Current / Incoming / Both**
2. 解完後：`git add <檔案>` → `git commit`（訊息 git 已預填好）
3. 繼續執行後續的 push 步驟

### 替代方案：rebase（歷史較乾淨）

```bash
git checkout main && git fetch upstream
git rebase upstream/main      # 把本地 main「重放」到最新之上（無多餘 merge commit）
git push origin main
```

> ⚠️ `branch-randy` 合併時仍建議用 **merge**——已 push 的功能分支做 rebase 容易出問題。

---

## 四、日常開發流程（在 branch-randy 上改程式）

```bash
# 1. 確認在功能分支
git checkout branch-randy

# 2. 修改程式碼後提交
git add .
git commit -m "說明你的修改"

# 3. 推到 fork
git push                        # 自動推到 origin/branch-randy

# 4. 要發 Pull Request 到原始 repo：
#    https://github.com/RC38/mindshub/pull/new/branch-randy
#    （從 RC38:branch-randy → mindsdb:main）
```

---

## 五、Submodule 相關注意事項

- 子模組預設是 **detached HEAD**（直接停在某個 commit）。要在子模組裡開發時先開分支：
  ```bash
  cd frontend && git checkout -b my-feature
  ```
- 同步主 repo 更新時，加上 `--recurse-submodules` 會連同子模組一起更新：
  ```bash
  git pull --recurse-submodules
  # 或
  git fetch upstream && git merge upstream/main --recurse-submodules
  ```
- 若子模組指向的 commit 有變動，merge 後執行 `git submodule update` 讓本地子模組跟進。

---

## 六、常用指令速查表

| 目的 | 指令 |
|------|------|
| 查看目前分支與狀態 | `git status` / `git branch --show-current` |
| 查看所有 remote | `git remote -v` |
| 拉取原始 repo 最新（安全） | `git fetch upstream` |
| 檢查是否有新 commit | `git rev-list --count main..upstream/main` |
| 同步完整流程 | 見「三、日常同步流程」 |
| 下載子模組 | `git submodule update --init` |
| 子模組狀態 | `git submodule status` |
| 建立新分支並推送 | `git checkout -b <名稱> main && git push -u origin <名稱>` |
| 取消未完成的 merge | `git merge --abort` |
| 查看最近 commit | `git log --oneline -10` |
