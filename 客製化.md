# MindsHub (Cowork) 部署與繁體中文化開發指引

本目錄為 MindsHub (MindsDB Cowork) 容器化部署與原始碼開發環境。

---

## 📂 目錄結構

```
mindshub/                 # MindsHub (Cowork) Git Superproject，backend/frontend 在頂層（無 src/ 子目錄）
├── .env                  # 環境變數設定（dev compose 讀取：COWORK_WEB_PORT / COWORK_API_PORT / MINDSHUB_DIR）
├── dev.env.example       # Makefile 模組 branch 選擇範本（cp 成 gitignored 的 dev.env；與 docker compose 無關）
├── docker-compose.yml    # 生產/標準模式（容器編譯並以 Nginx 提供服務，固定 3000:80 / 26866，不讀 .env）
├── docker-compose.dev.yml# 開發/中文化模式（前端熱重載、原始碼目錄即時映射；host ${COWORK_WEB_PORT:-15173}:5173）
├── Dockerfile.api.dev    # api dev image 建置檔（含預裝「資料分析 + DB driver」套件層）
├── export_mindshub_images.sh        # 匯出 image tar（離線部署用）
├── load-and-push-mindshub-to-harbor.sh  # 載入並 push 到 Harbor registry
├── Makefile              # make use / dev / server / app（搭配 dev.env 的 branch 工作流）
├── docker/               # 生產模式 Dockerfile（api.Dockerfile、web.Dockerfile、nginx.conf）
├── docs/                 # 官方文件（setup.html、use-cases.html…）
└── backend/              # Git submodules
    ├── core_agent/       # Anton Agent 核心 (mindsdb/anton)
    ├── core_api/         # Cowork API Server (mindsdb/cowork-server)
    └── data-vault/       # 資料庫與連線管理 (mindsdb/data-vault)
frontend/                 # React + Vite 前端 UI（submodule: mindsdb/cowork；修改繁體中文的主要目錄）
```

> **注意**：本 fork 的 `backend/`、`frontend/` 直接位於頂層，**沒有** `src/` 子目錄。
> 舊版部署副本 `05.mindshub/src/`（無 .git）已棄用，改動源頭一律在本 repo 的 `branch-randy`。

---

## 🚀 快速啟動

### 方式 A：開發與中文化模式 (建議修改繁中時使用)

此模式會將主機的 `frontend/` 與 `backend/` 原始碼（頂層目錄，無 src/）即時映射進容器，並啟用 Vite HMR（熱模組替換），儲存檔案後瀏覽器即可即時看到修改效果。

```bash
cd /Users/randychen/workspace/github/mindshub   # 開發環境 macbook（伺服器路徑見 .env MINDSHUB_DIR 註解）
docker compose -f docker-compose.dev.yml up -d
```

> **⚠️ bind mount 必須用宿主機絕對路徑**：`.env` 的 `MINDSHUB_DIR` 是 docker daemon（跑在宿主機）看得到的路徑。
> dev container / 相對路徑對 daemon 不存在，會導致容器 crash loop（package.json ENOENT）。

> **⚠️ 修改 `Dockerfile.api.dev`（例如新增預裝套件層）後，必須重新 build image 才會生效：**
>
> ```bash
> docker compose -f docker-compose.dev.yml up -d --build api   # 只重建 api 的 image（約 1~3 分鐘）
> ```
>
> - 僅 `up -d` / `--force-recreate` 會沿用「舊 image」重建容器，**不會**套用 Dockerfile 的新層。
> - 修改 `frontend/`、`backend/` 原始碼則**不需要** rebuild（bind mount + Vite HMR / hot reload 即時生效）。
> - build 時需能連到 PyPI；離線環境請先設定內部 mirror 或 vendored wheels，否則該層會失敗。
>
> **驗證預裝套件是否已進 image：**
>
> ```bash
> docker exec mindshub-api-dev python -c "import pandas, duckdb, pyarrow, pymysql, psycopg2; print('ok')"
> ```

- **前端 Web 介面**：`http://<主機IP>:15173`（`.env COWORK_WEB_PORT=15173`；5173 已被 open-webui-frontend 佔用，勿混用）
- **後端 API 介面**：`http://<主機IP>:26866`

---

### 方式 B：標準建置模式 (完成修改後正式運行)

此模式會進行正式打包編譯，以 Nginx 服務前端靜態頁面：

```bash
docker compose -f docker-compose.yml up -d --build
```

---

## 🌐 繁體中文化修改指南

前端所有介面原始碼均位於頂層 `frontend/`（submodule，RC38/cowork branch-randy）：

### 1. 主要修改位置

- **主入口與全域頁面**：
  - `frontend/src/renderer/index-web.html`（網頁 Title 與 `<html lang="zh-TW">`）
  - `frontend/src/renderer/CoworkApp.tsx`
  - `frontend/src/renderer/App.tsx`
- **各子頁面與功能模組**：
  - `frontend/src/renderer/pages/`
  - `frontend/src/renderer/cowork/`
  - `frontend/src/renderer/lib/`

### 2. 即時修改與驗證

在開發模式 (`docker-compose.dev.yml`) 運行狀態下：

1. 直接以 VS Code 編輯 `frontend/src/renderer/` 底下的 `.tsx` 或文字內容。
2. 存檔後切換至瀏覽器 `http://<主機IP>:15173`，網頁將自動即時熱更新。

---

## 🛠️ 常用管理指令

- **查看即時日誌**：
  ```bash
  # 前端日誌
  docker compose -f docker-compose.dev.yml logs -f web
  # 後端 API 日誌
  docker compose -f docker-compose.dev.yml logs -f api
  ```
- **停止服務**：
  ```bash
  docker compose -f docker-compose.dev.yml down
  ```
